#ifndef VALIDATION_H
#define VALIDATION_H

/**
  * Determines if there is donut is setup correctly in the current path.
  * @returns If donut is initialized 1 is returned otherwise 0
  */
int validate_donut_repo(void);

#endif
